@model IEnumerable<Szkola.Models.OcenaKursu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Student.ImieStudent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdKurs)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ocena)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Student.ImieStudent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdKurs)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ocena)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdOcena }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdOcena }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdOcena })
        </td>
    </tr>
}

</table>
