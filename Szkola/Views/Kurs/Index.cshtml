@model IEnumerable<Szkola.Models.Kurs>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LStudentów)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LStudentów)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdKurs }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdKurs }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdKurs })
        </td>
    </tr>
}

</table>
